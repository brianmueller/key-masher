### To-Do:
* Make background change color/shade after each second that passes while typing
* Fix bug on mobile: user can keep pressing keys after 3s
* Optimize layout for mobile
* Add SQL backend for scoreboard
